import { Routes } from '@angular/router';
import { RepoTrackerComponent } from './components/repo-tracker/repo-tracker.component';

export const routes: Routes = [
  { path: '', component: RepoTrackerComponent },
];
